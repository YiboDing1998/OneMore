/**
 * 与 Web 项目的关系说明
 */

# 🌐 Web vs Mobile 项目关系

## 项目概览

你现在有两个项目：

```
Desktop/Fitness App Development/
├── Fitness App/                # Web 版本 (Vue/React)
│   ├── src/
│   ├── package.json
│   └── vite.config.ts
│
└── FitnessAppMobile/           # 移动版本 (React Native)
    ├── src/
    ├── package.json
    └── app.json (Expo)
```

## 代码复用策略

### ✅ 可以复用的内容

| 内容 | Web | Mobile |
|------|-----|--------|
| **类型定义** | ✅ | ✅ |
| **常量** | ✅ | ✅ |
| **工具函数** | ✅ | ✅ |
| **业务逻辑** | ✅ | ✅ |
| **API 客户端** | ✅ | ✅ (轻微修改) |
| **验证规则** | ✅ | ✅ |
| **数据转换** | ✅ | ✅ |

### ❌ 不能直接复用

| 内容 | 原因 |
|------|------|
| **UI 组件** | Web 和 Mobile API 不同 |
| **样式** | CSS vs StyleSheet |
| **导航** | React Router vs React Navigation |
| **存储** | localStorage vs AsyncStorage |
| **屏幕布局** | 不同的设备特性 |

## 代码共享方案（可选）

### 方案 1: 单仓库多包 (Monorepo)

```
fitness-app/
├── packages/
│   ├── web/                 # Web 项目
│   ├── mobile/              # Mobile 项目
│   └── shared/              # 共享代码
│       ├── types/
│       ├── constants/
│       ├── utils/
│       └── services/
├── package.json (workspace)
└── tsconfig.json
```

**优点**：
- ✅ 代码共享方便
- ✅ 统一的依赖管理
- ✅ 容易保持代码同步

**缺点**：
- ❌ 配置复杂
- ❌ 构建时间长

### 方案 2: npm 包

将共享代码发布为私有 npm 包：

```
shared-fitness-core/
├── src/
│   ├── types/
│   ├── constants/
│   ├── utils/
│   └── services/
├── package.json
└── dist/

# Web 和 Mobile 都依赖这个包
npm install shared-fitness-core
```

**优点**：
- ✅ 清晰的版本管理
- ✅ 独立的开发周期

**缺点**：
- ❌ 发布流程复杂
- ❌ 版本同步困难

### 方案 3: 复制粘贴（简单方案）

对于小项目，直接复制常用文件：

```bash
# 从 Web 项目复制到 Mobile
cp Fitness\ App/src/core/* FitnessAppMobile/src/core/
cp Fitness\ App/src/utils/* FitnessAppMobile/src/core/utils/
```

**优点**：
- ✅ 简单快速

**缺点**：
- ❌ 同步困难
- ❌ 代码重复

## 推荐方案

对于你的情况，**推荐使用方案 2 (npm 包)** 或 **单仓库方案**：

### 实现步骤（如果使用 npm 包方案）

**1. 创建共享包**
```bash
mkdir shared-fitness
cd shared-fitness
npm init -y
npm install typescript
```

**2. 编写共享代码**
```
shared-fitness/
├── src/
│   ├── types/     # 类型定义
│   ├── constants/ # 常量
│   ├── utils/     # 工具函数
│   └── services/  # 业务服务
├── dist/
└── package.json
```

**3. 在 Web 和 Mobile 中使用**
```bash
# Web
npm install ../shared-fitness

# Mobile
npm install ../shared-fitness
```

**4. 导入**
```typescript
// 在 Web 或 Mobile 中
import { User, useAuth } from 'shared-fitness';
```

## 架构一致性

即使代码不共享，两个项目也应该保持架构一致：

```
Web (Fitness App)
├── presentation/        Mobile (FitnessAppMobile)
├── business-logic/      ├── presentation/
├── data/                ├── business-logic/
└── core/                ├── data/
                        └── core/
```

**好处**：
- ✅ 团队成员容易切换项目
- ✅ 知识可以迁移
- ✅ 最佳实践一致

## API 契约

两个项目都与同一个后端 API 通信。确保 API 契约清晰：

```typescript
// src/core/types/api.ts - 两个项目都有
export interface LoginRequest {
  email: string;
  password: string;
}

export interface LoginResponse {
  user: User;
  token: string;
}

export interface ApiError {
  code: string;
  message: string;
}
```

## 开发流程

```
后端 API
  ↑       ↑
  │       │
Web   Mobile
(不共享代码)
```

**协调方式**：
1. **API 优先设计** - 先确定 API 接口
2. **本地模拟** - 开发时使用 Mock API
3. **集成测试** - 定期进行端到端测试
4. **版本兼容** - 确保 API 版本兼容

## 建议

### 短期（第一个月）

- 保持两个项目独立
- 重点是各自的功能完成
- UI/UX 可以不完全相同

### 中期（1-3 个月）

- 建立共享代码仓库（如果有大量可复用代码）
- 统一架构和命名设
- 同步常见工具函数

### 长期（3+ 个月）

- 考虑 Monorepo 或 npm 包化
- 建立 CI/CD 管道同时测试 Web 和 Mobile
- 共享组件库（如果可能）

## 最终建议

现阶段，建议：

1. **保持独立开发** - 各个项目各自完成
2. **维持代码一致性** - 相同的业务逻辑采用相同的实现
3. **建立清晰的 API 契约** - 避免 Web 和 Mobile 对 API 的理解不同
4. **定期同步** - 团队内讨论架构和最佳实践
5. **提前规划共享** - 如果项目扩大，准备好共享代码的方案

---

有任何问题，欢迎讨论！ 🚀
